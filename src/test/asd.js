/*
class HaxMap {
  constructor (playersPerTeam, identifier) {
    this.playersPerTeam = playersPerTeam;
    this.identifier = identifier;
    this.positionsRed = this.createPositionsArray(playersPerTeam);
    this.positionsBlue = this.createPositionsArray(playersPerTeam);
  }
  createPositionsArray(length) {
    let arr = [];
    for (let i = 0; i < length; i++) arr[i] = 0;
    return arr;
  }
}

let randomMap = new HaxMap(3,'randomMap');

const allowPositions = {
  3 : 
}

*/
