
var room = HBInit();

room.pluginSpec = {
  name: `...`, // Current Map Original Disc Properties
};

class Game {
  constructor(){
    let GAME_STATE;
    this.getGameState = () => GAME_STATE;
  }
}

const GM = new Game();
